<%- include("partials/header") %>


<div class="container">
  <% if(error && error.length > 0) { %>
  <div class="alert my-5 alert-danger"><%= error %></div>
  <% } %> 
  
  <% if(success && success.length > 0) { %> 
    <div class="alert my-5 alert-success"><%= success %></div>
  <% } %>

  <div class="px-4 py-5 my-5 text-center">
    <h1>you have reached the secrets page</h1>
  </div>

  <a
    type="button"
    class="link bg-success text-white btn btn-lg px-4"
    href="/logout"
    >logout
  </a>
   <a
        type="button"
        class="link bg-secondary text-white btn px-4"
        href="/"
        >home</a
      > 
</div>

<%- include("partials/footer") %>
